apiVersion: apps/v1
kind: Deployment
metadata:
  name: grupo26
  namespace: thymeleaf
spec:
  selector:
    matchLabels:
      app: grupo26
  replicas: 1
  template:
    metadata:
      labels:
         app: grupo26
    spec:
      imagePullSecrets:
         - name: github-secret
      containers:
         - name: grupo26
           image: localhost:5000/grupo_2_6:latest
           ports:
              - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: grupo26
  namespace: thymeleaf
spec:
#  type: NodePort
  selector:
    app: grupo26
  ports:
    - protocol: TCP
      port: 8080
#      nodePort: 30001
      targetPort: 8080